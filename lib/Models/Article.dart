class Article{

  int id;
  String title;
  String description;
  String image;
  double price;
  String size;
  String collection;

  Article(this.id, this.title, this.description, this.image, this.price,
      this.size, this.collection);
}